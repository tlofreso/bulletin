services:
  ocr-server:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Persist Hugging Face model cache (~2GB) across restarts
#      - hf-cache:/root/.cache/huggingface
      - /mnt/user/appdata/ocr:/root
      - /mnt/user/appdata/ocr/python:/usr/local/lib/python3.11
    deploy:
      resources:
        reservations:
          devices:
          - driver: nvidia
            count: all
            capabilities: [gpu]
    restart: unless-stopped

#volumes:
#  hf-cache:
